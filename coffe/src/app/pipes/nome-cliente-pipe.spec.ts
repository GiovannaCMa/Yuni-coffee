import { NomeClientePipe } from './nome-cliente-pipe';

describe('NomeClientePipe', () => {
  it('create an instance', () => {
    const pipe = new NomeClientePipe();
    expect(pipe).toBeTruthy();
  });
});
